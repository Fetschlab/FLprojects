import numpy as np
import matplotlib.pyplot as plt

class Neuron:
    def __init__(self, spiketimes, samplerate, amps=None, waveforms=None, template=None, temp_amp=np.nan,
            sessionID='', type='', area='', channel=0, depth=np.nan, electrode=''):
         # change some to kwargs?
        self.spiketimes = np.array(spiketimes)
        self.amps = amps
        self.template = template
        self.temp_amp = temp_amp
        self.sessionID = sessionID
        self.type = type
        self.area = area
        self.channel = self.channel
        self.depth = depth
        self.electrode = electrode

    def __str__(self):
        return f'{type} {self.area} unit, {len(self.spiketimes)} spikes, session {sessionID}, \
            ch{self.channel} at {self.depth} depth, using {self.electrode}'

    #def compute_isi(self, binsize=0.01, max=0.2):

    # compute_corr(self, binsize=0.01, max=0.1):

    # plot_waveforms(self):
        

    # wf_widths(self):
    
    # compute_psth(self, alignEvent, binsize=0.01, trange=[-1,1]):

    # raster_psth(self, alignEvent, binsize=0.01, trange=[-1,1]):


if __name__ == '__main__':
    """
    do some diagnostics
    generate a neuron with poisson firing, and some modulation
    """
    neuron = Neuron()